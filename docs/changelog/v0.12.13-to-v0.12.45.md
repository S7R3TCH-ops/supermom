# Changelog v0.12.13 – v0.12.45

Brief summaries. For diffs use `git log --oneline` + `git show <sha>`.

- **v0.12.45** — Settlement total moved to top of "Also Paid" block; removed redundant cream bar; PDF totals row bottom-aligned
- **v0.12.44** — PDF Payments Received column: `flex:1` → `width:160`
- **v0.12.43** — Payment list `maxWidth:210px` + `alignItems:flex-end`; "Invoice/Receipt Ready" banner labels; pre-push hook → hard block
- **v0.12.42** — Admin "Mark as Unpaid" button in JobDetailSheet; receipt layout: payments left of totals, `✓ Paid` before amount, settlement total line
- **v0.12.41** — Settle-all-outstanding from invoice page; multi-job receipt with "Also Paid" section; `settleInvoiceOutstanding` + `voidInvoiceSettlement` in invoicesRepo; owner-gated payment panel in InvoiceView
- **v0.12.40** — Second double-HST fix: PostJobSheet `liveTotal` now uses `flat_rate ?? subtotal` not `total_amount`; `repair-double-hst.mjs` script; fixed Ann Rae's 3 jobs
- **v0.12.39** — First double-HST fix in `financialMath.js`: removed `src?.total_amount` fallback, replaced with `src?.subtotal`
- **v0.12.38** — Financial math audit: Finance.jsx Outstanding stat subtracts partial payments; `toDisplayJob()` uses `computeJobTotal()`; Home "Collected This Week" HST-consistent
- **v0.12.37** — Invoice totals cohesion: color/font normalization; "Outstanding Balance" hidden when no payments, renamed to "Remaining"/"✓ Paid in Full"
- **v0.12.36** — "Also Outstanding" section on invoice; client street address in "Issued To"
- **v0.12.35** — Settings save crash fix; client picker address build fix; blank initials fix; client name → profile nav in JobDetailSheet
- **v0.12.34** — Schedule page P1+P2 polish: 44px hit areas, skeleton loading, conflict banner, empty state CTA; removed Swipeable delete
- **v0.12.33** — Remove Georgetown default; NewClientSheet double-submit fix + full Intel section; richer Read Brief; Calendar week-scoped agenda; GCal location/description enrichment
- **v0.12.32** — Android perf fix: `cachedEffectiveId` in `currentBusiness.js`; GPS `maximumAge:90000`; Vercel native GitHub integration confirmed (no deploy.yml needed)
- **v0.12.31** — Per-job HST toggle (`jobs.tax_enabled` nullable); removed EST labels; CSV export in Finance
- **v0.12.30** — Calendar week grid hidden; Agenda is only view; WeekView renamed `_*_PARKED`
- **v0.12.29** — PWA TDZ crash fix: moved `useState` declarations above the `useEffect` that lists them as deps in Home.jsx
- **v0.12.28** — Finance page critique pass: dark plum toggle, skeleton loading, removed activity cap, disabled dead CTAs
- **v0.12.27** — Settings page critique pass: dark mode tokens, address/city/postal inputs, ToggleSwitch, isDirty state, Sign Out button
- **v0.12.26** — Circular import fix: extracted `notifyDataChanged`/`CHANGE_EVENT` to `events.js`; SW skipWaiting + cache clear on activate
- **v0.12.25** — Leave-time push notifications via SW setTimeout; permission banner in Home; `injectManifest` mode
- **v0.12.24** — Function consolidation: maps + invoice endpoints; PWA support with vite-plugin-pwa
- **v0.12.23** — Settings error state for unprovisioned accounts; provisioned `joel@test.com`
- **v0.12.22** — Dark mode job cards using theme tokens
- **v0.12.21** — Production launch: data wipe + Sandra re-provisioned; service pricing type propagation fixes
- **v0.12.20** — Invoice/Receipt label large+bold; distance columns written to jobs; v0.12.19 committed
- **v0.12.19** — Invoice/Receipt two-stage flow; auto-generates on completion; `invoice_sent_at`/`receipt_sent_at` stamps
- **v0.12.18** — Cross-session git sync: auto-push/pull hooks; `/wrap` stop hook; `cc` PowerShell wrapper
- **v0.12.17** — Invoice PDF polish: address block restructured, logo enlarged; `download-invoice.js` for cross-platform filename fix
- **v0.12.16** — Home screen critique fixes: contrast, hit targets, fontWeight, GO button, SectionLabel
- **v0.12.15** — Invoice print one-page; `✓ Paid` no-wrap; PDF filename includes client last name
- **v0.12.14** — `postal_code` field; invoice shows real payment status; "Other Outstanding Balances"; `due_date` same-day
- **v0.12.13** — Calendar overhaul; team labels → Sidekick/Wingmom; Home UX overhaul; duplicate GCal events fixed; PDF on email
